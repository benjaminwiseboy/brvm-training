import { Onboarding } from "@/components/onboarding/Onboarding";

// Pas d'<AppShell> : page plein écran (carrousel M0), cf. task-12-brief.md.
export default function OnboardingPage() {
  return <Onboarding />;
}
